# 开发流程指引（13 课路线图）
> 本文件回答「**现在该做什么**」。
> 最后更新：**2026-09-10（全篇重写：对齐代码实况）**
> 来源：`docs/开发指导/` —— **只学流程，不抄其中的具体游戏案例**（DEC-023）

> ⚠️ **本文件曾在 9/1 – 9/10 间严重滞后**（还写着「你在这里 = 第⑤步」，而实际上 P0 已近收尾）。
> 滞后会让 AI 按错误的进度给建议 —— 请遵守这条维护纪律：
> **每完成一个 BACKLOG 步骤，回来只改两处：「§一 你在这里」与「§七 下一步」。**
> 其余四节（二 / 三 / 四 / 五 / 六）是**方法论**，基本不动。

---

## 一、你现在在哪里

### 13 课路线图

```
阶段一 · 基础入门（01–05）                                    ✅ 全部完成
  ✅ 第 01 课  灵感与方向        → 三问法已过，方向已定
  ✅ 第 02 课  Bible 体系        → docs/ + bible/ 已建
  ✅ 第 03 课  工具链            → Godot 4.7 工程 + 自编译 godot-mcp
  ✅ 第 04 课  第一个可玩版本     → 白盒可跑，核心循环闭环
  ✅ 第 05 课  功能拆解与优先级   → MVP_SCOPE.md + BACKLOG.md

阶段二 · 进阶实战（06–08）
  🟡 第 06 课  视觉资产          → ⭐ **当前头号重点**（见下方「当前唯一动作」）
  ⬜ 第 07 课  主视觉与短片
  ⬜ 第 08 课  关卡与数值策划

阶段三 · 打磨发布（09–13）    ⚠️ 本项目**不是线性推进**，见下方注
  🟡 第 09 课  Game Feel 五层    → ②闪白 ③震屏 ④推镜 ⑤血量反馈 ✅；余 ⑥VFX + ①hitstop
  ✅ 第 10 课  音频               → **已提前做掉**（DEC-046）
  ⬜ 第 11 课  第一次外部测试
  ⬜ 第 12 课  性能与体验
  ⬜ 第 13 课  组队（单人开发可跳过）
```

> **为什么第 10 课（音频）被提前做掉？** 因为音频属于「后期再加会到处打补丁」的那类系统 ——
> 骨架必须在模块定型之前搭好（第 10 课自身的观点）。同理，第 09 课的 Game Feel 也是按「一次一层」提前切入的。
> **路线图是清单，不是时刻表；决定顺序的是「哪件事晚做代价最高」。**

### BACKLOG 12 步进度

> 编号以 `BACKLOG.md` 的**开发顺序表**为准；代码侧的测试场景命名沿用旧序号，别混淆。

```
✅ ① 工程 + 白盒舰桥（单面舷窗）+ 第一人称转头
✅ ② EventBus / GameStateManager 骨架
✅ ③ 六扇区 + 敌人生成与移动
✅ ④ 炮塔开火（主炮固定 + 4 副炮 + 接管切视角）
✅ ⑤ 炮塔耐久 + 被毁 + 全毁 = 失败 + 结算
⏸ ⑥ 监控接真副炮视角        —— **冻结**，等美术模型定尺寸（DEC-038）
✅ 波次调度（原缺编号）       —— `wave_system.gd` + `waves.json`
✅ ⑦ 改装阶段（换炮塔 / 扩展槽位）
✅ ⑧ 自动解锁（随危机进度）
✅ ⑨a 战损报告（一套数据、四个出口）
✅ ⑨b 陷落演出三级分层
✅ ⑩ 音频骨架
🟡 ⑪ Game Feel 五层 + VFX    —— ②③④⑤ 完成，余 ⑥ VFX + ① hitstop
```

**P0 只剩两项：⑪ 的 VFX 与 hitstop。**（详细验收口径见 `BACKLOG.md` 的进度快照表。）

### 当前唯一动作：**定美术资产方案**

P0 收尾后的头号重点是美术 —— 它是后续 P1 / P2 的地基，而且**卡着 ⑥ 的解冻**。

| | |
|---|---|
| **范围** | 按 `bible/04_presentation` 的替换顺序：**舱室 → 控制台 → 敌人 → 炮塔 → VFX** |
| **首要解锁** | **⑥ 监控接真副炮**（等模型定尺寸）—— 模型一定，⑥ 解冻 |
| **谁主导** | **李**。风格定稿、选型决定、商业资源谈判**不在 AI 范围内** |
| **AI 边界** | 定方向、找候选、写对比表 ✅ |

---

## 二、开工前体检（第 04 课 · 每天开工前过一遍）

| # | 检查项 | 怎么做 |
|---|--------|--------|
| 1 | Godot 能打开工程 | 双击 `project.godot`，能进入编辑器 |
| 2 | 能跑 | F5 能启动，不报错 |
| 3 | 场景树正常 | 主场景节点结构符合预期 |
| 4 | MCP / AI 指向正确 | AI 的工程路径指向 `D:\GameProject\Starsea Helm` |
| 5 | 输出面板开着 | 编辑器 → 编辑器面板 → 输出（报错第一时间看到） |

> 工程路径含空格（`Starsea Helm`），若 godot-mcp 出问题，先排查是不是空格导致的。

---

## 三、三轮对话节奏（第 05 / 11 课 · **这是你跟 AI 协作的核心方法**）

> 大多数 AI 协作失败，都是因为跳过了第一轮直接要代码。

| 轮次 | 你做什么 | AI 做什么 | 禁止 |
|------|---------|----------|------|
| **第一轮** | 说清任务 | **只复述理解 + 给方案**，不写代码 | ❌ 不要让 AI 直接写代码 |
| **第二轮** | **只回答 AI 提的疑问**，不加新需求 | 确认方案细节 | ❌ 不要在这一轮追加新想法 |
| **第三轮** | 说「开始实现」 | 实现 + 自测 | ❌ 不要中途改需求 |

### 第一轮可以直接复制的模板

```
我先说任务，你不要写代码。

【背景】项目 Starsea Helm，Godot 4.7，第一人称舰桥据点防守。
先读 docs/bible/_index.md，按它说的只读本次相关的文件。

【任务】
（一句话说清要做什么）

【约束】
- 复述你对任务的理解，先不要写代码
- 涉及新实体查 02_entities.md，调数值查 03_rules.md，改流程查 01_core_loop.md
- 数值一律外置到 data/*.json，禁止 hardcode
- 一次只改一件事

【我的疑问】
（列出你不确定的点，让 AI 反过来问你）
```

---

## 四、调试心法（第 04 课）

**不要说「不工作」，要说「看到了什么」。**

| ❌ 这么说 | ✅ 这么说 |
|----------|----------|
| 「开火不工作」 | 「点击接管后，控制台报 `Invalid call. Nonexistent function 'fire' in base 'Node'`，第 42 行」 |
| 「敌人不动」 | 「敌人生成了但停在原地，Inspector 里 speed 显示 0，data/enemies.json 里写的是 8」 |
| 「报错了」 | 「贴完整报错文本」 |
| 「感觉不对」 | 「主炮自动 DPS 比手动还高，但我期望手动接管明显更快（03_rules.md 里 manual_dps > auto_dps）」 |

**描述三要素**：表象（看到了什么）→ 位置（哪个脚本/哪个节点/第几行）→ 期望（应该是怎样）。

---

## 五、不可违反的铁律（贯穿全课程）

| # | 铁律 | 出处 |
|---|------|------|
| 1 | **不写 Bible 就用不了 AI**。改了设计就回填文档 | 第 02 课 |
| 2 | **先让 AI 说方案，再让它写代码** | 第 02 课 |
| 3 | **一次只改一件事**，改完自测再改下一件 | 第 09 / 12 课 |
| 4 | **参数外置到 JSON / @export**，AI 只给「旋钮」不给「数值」 | 第 09 课 |
| 5 | **不要 hardcode** | 第 09 课 |
| 6 | **看毫秒，不看 CPU%**（80% 占用但帧率达标就没事） | 第 12 课 |
| 7 | **朋友玩 3 分钟还想再玩 = MVP 成立** | 第 01 / 05 课 |
| 8 | **核心循环里没出现的动词，一律砍掉** | 第 05 课 |
| 9 | **AI 帮你排优先级，但拍板在人类** | 第 05 课 |
| 10 | **知识库只学流程，具体游戏案例不照抄** | DEC-023 |

> **第 3 条在代码层的具体化**（踩过坑，见 `.workbuddy/memory/testing.md`）：
> 改完必须**实跑确认** `errors: []`，不许凭「我以为改了」报成功；
> 测试要**走真实调用路径**（如伤害调 `Turret.take_damage` 而不是直接 `emit`，否则坏了也是绿的）。

---

## 六、本项目的三条最高宪法（贴在显示器上）

1. **70% 乐趣在改装阶段，30% 在战斗阶段。**
   战斗刻意低 APM，「挂机感」是目标不是缺陷。
   真正的失败模式是「决策无意义」，不是「玩家不动手」。

2. **知识库提供流程，不提供答案。**
   三问法、Bible、P0–P3、一次只改一件事 —— 这些必须照做。
   弹珠肉鸽、Vampire Survivors、Brotato —— 这些只是教学示例。

3. **数值只给旋钮不给终值。**
   AI 给的初值几乎都不对，进引擎自己调。

---

## 七、下一步

```
✅ 已完成（不要再回头做）
  BACKLOG ①–⑧ · ⑨a · ⑨b · ⑩ · ⑪ 的 ②③④⑤

➡️ 当前唯一动作：美术资产方案（李主导）
   替换顺序：舱室 → 控制台 → 敌人 → 炮塔 → VFX
   解锁：⑥ 监控接真副炮（等模型定尺寸，DEC-038）

⏸ 之后（P0 收尾，顺序可互换）
   ⑪ ⑥ VFX      —— 程序化占位，不必等美术
   ⑪ ① hitstop  —— ⚠ **禁用 `Engine.time_scale = 0`**：
                    改关 `EnemySystem` / `TurretSystem` 的 `_physics_process`（世界推进集中式）

⏸ 再往后（阶段二）
   第 07 课 主视觉与短片 · 第 08 课 关卡与数值策划
```

> ⚠️ 未决问题看 `BACKLOG.md` 与 `bible/04_presentation.md` 的「未决」小节
> （例如：来袭时 `fore` 会先啃 `dorsal` 而非主炮 —— bible 的「最近」未限定扇区）。

---

## 调试快捷键（白盒当前可用）

`1`–`5` 接管对应炮位（1 主炮 / 2 左 / 3 右 / 4 上 / 5 下，8 炮位按路循环；**再按同键退出**）·
`Space` 或鼠标左键 开火 · `B` 进入 BATTLE（B 键 BATTLE 入口）· `E` 随机扇区生成敌人 ·
`R` 全部生成在 `fore` · `F` 敌人可见性诊断 · `ESC` 退接管 / 释放鼠标 / 退出

改装阶段（维修站过场）：`ESC` 跳过当前阶段 · `Enter` 确认 · `0`–`9` 选择型号

> 这些是白盒期的调试入口，接入正式 UI 后该删的会删。
