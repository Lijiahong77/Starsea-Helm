# 2026-09-06

## 上午：全文档评测 + 5 分钟自测审计
> 本日不写新功能，**只复盘**。用户拍板：先停下 5 分钟做个自测审计，列具体问题，决定下一步。

### 1. 读了哪些文档
- `docs/BACKLOG.md`（P0-P3 + 12 步开发顺序）
- `docs/bible/01_core_loop.md`（状态机 + 攻陷链 + 主轴 70/30）
- `docs/bible/99_decisions.md`（DEC-014~039 + Q9-Q14 待决）
- `docs/bible/_index.md`（AI 会话入口 + Context Prelude）
- `docs/MVP_SCOPE.md`（P-1/P-2/P-3 设计假设验证点）
- `docs/PROCESS.md`（13 课路线 + 当前定位）
- `docs/EVALUATION.md`（旧评估，已严重滞后）
- `docs/开发指导/知识库索引.md`（流程手册导览）

**没读**（选择性的，等触到相关任务再看）：
`SPEC.md` / `EXPERIENCE.md` / `MODULES.md` / `02_entities.md` / `03_rules.md` /
`04_presentation.md` / 阶段一/二/三知识库分册。

### 2. 关键判断
- **BACKLOG 进度**：① ② ③ ④ R1 完成，共 4/12 步。④ 整体 = 主炮 Round 1 已端到端、
  副炮 Round 2 待做。
- **Prototype 6 条验收**（MVP_SCOPE 第一节）：
  - ①② 主控室 / 六扇区 + 敌人来袭并被击毁 ✅
  - ③ 主炮固定 + 副炮×4 自动 + 可接管 → **半通**（主炮通，副炮没接弹药 / 接管切主相机）
  - ④ 炮塔耐久 + 被毁 + 全毁 = 失败 → **没动**
  - ⑤ 监控 = 副炮视角 → **半通**（4 路 SubViewport 在跑但相机绑标记，不是绑真副炮）
  - ⑥ FPS ≥ 55 → 未实测
- **P-1/P-2/P-3 验证**：P-1 ≈ 50% / P-2 0% / P-3 0%（P-3 要等 ⑤ + ⑥ + ⑦ 跑通才能跑 A/B 装配 vs 随机）

### 3. 实测 Godot 启动报告
跑 `mcp__godot__run_project + get_debug_output`，干净启动：
- Vulkan 1.4.351 / RTX 5070 Laptop
- stdout 4 行：enemy 配置就绪 / 六面权重 / turret 系统就绪 1 门 / 默认 AUTO
- stderr 5 条 WARNING（非 ERROR，未 fail 编译）：
  1. `enemy.gd:101` `take_damage` 参数 `source_id` 未使用（预留 ⑤）
  2. `enemy.gd:130` `_build_mesh(size)` 局部 size 遮蔽类成员 `var size`
  3. `turret.gd:44` `var range` 与 built-in `range()` 重名
  4. `turret.gd:153` `_fire()` 参数 `target` 未使用
  5. `turret.gd:179` `var aim_point` 声明但未使用

### 4. 已识别的技术债 / 功能缺口
- ⚙ 一致性 / 命名（5 条 WARNING）
- 📊 turrets.json sub_gun_instances mount.z 全 -0.6，应按 hull_radius × axis 推导
- 🎯 Round 2（4 副炮 + 切主相机）/ ⑤（炮塔耐久 + RESULT）/ ⑥（feed 接真副炮）
- 📄 PROCESS.md / EVALUATION.md 文字滞后
- ❓ Q13 同位置多副炮火力聚合（用户："等 ⑦ 改装再做"）

### 5. 用户拍板
**自测审计选项 D 中选**：先停下 5 分钟做自测审计（出清单）。Q13 留 ⑦ 再说。

---

## 下午：Q13 定案 + 文档回填 + ④ Round 2 完成 + 注释自检

### 6. Q13 → DEC-040
**各自独立，不合并 DPS**。理由：异型炮混装下单一 DPS 抹平决策 → 违反 DEC-021。
约束：每门一个 Turret 实例，禁止共享 cooldown / 伤害累加通道 / 面 DPS 中间层。
已写 `99_decisions.md`（DEC-040 + Q13 标 ✅）。

### 7. 文档回填 + ④ Round 2（21/21 断言通过）
- `PROCESS.md` ④「进行中」+ 进度表 + 快捷键；`EVALUATION.md` 文首加状态对齐块，
  原「工程为零」结论作废。
- 4 副炮 mount = axis × hull_radius 推导（port/starboard ±11.5, dorsal/ventral ±8.5），
  id == 扇区；接管 emit `takeover_started/ended` → bridge 搬 Player（主炮 fore 不搬，DEC-026）。
- **3 个真 bug**（Round 1 被"看起来能玩"掩盖）：① 弹丸不注册进 `_projectiles`
  ② `_origin_pos` 未赋值 ③ 副炮 5m/帧 > 命中 3.8m 穿模（改线段检测）。
  顺带：`projectile.gd / turret.gd` 硬编码 `Bridge/EnemySystem` → group。
- ⚠ 教训：主炮能打死人 ≠ 链路对的；靠兜底路径跑通的功能，要在手动推进模式下重验。

### 8. 注释自检（用户问"注释写好了吗"）：4 处「注释与代码不符」— 全部已修
**问题不是「没写」，是「写了没跟着改」**：
1. `turret.gd:132` `acquire_target` 仍硬编码 `get_node("Bridge/EnemySystem")`（同 Projectile 笔债）→ 改 group。
2. `turret.gd:222` `_target_velocity` ship_center hardcode `Vector3(0,1.5,-1.3)`，注释也写「enemies.json」= 两份真相 → Enemy 加 `flight_target()` getter。
3. `turret_system.gd:65` 旧"清理超出 root"注释 → 删。
4. `projectile.gd:14` 文件头旧"遍历 EnemySystem"注释 → 改。
- ⚠ **改实现不改注释 = 埋雷**。注释里写了「数值来源/查找路径」的，实现一换注释变假情报。

### 9. 验证 + 新坑入册
- 回归：测试 **21/21**，0 error / 0 warning。
- headless 跑主场景刷 `Parameter "t" is null.` → dummy 渲染锅，GUI 不报。
  **已写进 `godot_pitfalls.md 第 12 节`**（判别 + 不要加 null 分支掩盖）。

### 10. 给用户的试玩清单（手玩前 5 分钟 + 手玩时报告）
**按键**：`1-5` 接管 main/port/starboard/dorsal/ventral（同键 = 退）·
`Space`/鼠标左键 = 扳机（仅接管后）· `E` 随机扇区 · `R` 定向 fore · `F` 可见性诊断 ·
`ESC` 释放接管/鼠标/退出。
⚠ **没有 BATTLE 入口** → 自动开火全程关，只能手动接管打。

**手玩前 5 分钟看**：`turret.gd:1-15` · `turret_system.gd:1-30` · `projectile.gd:1-15` ·
`turrets.json` `_doc` 字段 · `99_decisions.md DEC-040`。

**体感报告维度**：头控 / 接管 / 火力手感 / 命中穿模 / 数值曲线 / 4 路 feed 画面。

→ **下一步待用户拍板**：⑤ 炮塔耐久 / ⑥ 监控接真副炮 / 补 BATTLE 入口解锁 auto。
</content>
</invoke>