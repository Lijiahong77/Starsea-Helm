# 2026-09-08

## ⑨ 波次调度与难度曲线（P0）· 已完成

**用户裁决（开工前 AskUserQuestion）**：① 波内敌人**一次全出**（非分批）——
190m 飞行时间天然形成 bible 的预警(8s)→接敌→交战→清场 四子阶段；
② 难度**温和起步**——首波 4 只、每波 +1、第 4 波起掺 bomber。

**产出**（全部数值外置，`data/waves.json` 是唯一真源）：
- `data/waves.json`（新）：base_count / count_growth / max_count / hp_scale_per_crisis
  / bomber_start_crisis / bomber_ratio_{base,growth,max}。
- `scripts/systems/wave_system.gd`（新，class_name WaveSystem）：危机计数、
  `compose(idx)` 算组成（纯函数，可直接断言）、BATTLE 进 → 整波 spawn → emit
  `wave_started`、清空 → `crisis_cleared` + change_state(REFIT)、离开 BATTLE 清场、
  RESULT 后 wave_index 归零。
- `enemy_system.gd`：加 `SYSTEM_GROUP := &"enemy_system"` 并在 _ready 注册 ——
  系统节点之间**不硬编码路径**（沿用 projectile/turret 当年改 group 的修法）。
- `bridge_whitebox.gd` + `.tscn`：挂 WaveSystem 节点，HUD 加「危机 N（已撑过 M）+
  本波剩余 X/Y」；EventBus 的 wave_started / crisis_cleared 真正被 connect，
  按约定删掉这两行的 `@warning_ignore`。

**两个设计决定（写进代码注释了）**：
1. **清空判定只看自己 spawn 的敌人**（自存 `_wave_enemies`），不用
   `EnemySystem.enemy_count()` —— 否则主场景按几下 E 调试键，本波就永远清不空。
2. **离开 BATTLE 必须清场** —— 否则残留敌人 + 反复按 B（开战回满耐久）= 无限续命，
   ⑤ 的失败条件直接失效。

**验证**：`wave_test.tscn` **25/25 通过**；回归 `turret_hp_test` 30/30、
`turret_round2` 21/21；主场景 headless 冒烟无脚本错误
（仅 `Parameter "t" is null` 的 dummy 渲染噪声，坑 #12）。
→ 指针：`.workbuddy/memory/design_snapshot.md`（循环/波次）、`tooling.md`（测试场景表）。

**踩坑（已入 tooling.md 验证纪律）**：敌人是 `call_deferred queue_free` 的，
测试里跨阶段断言 `enemy_count()` 会把**上一波的尸体**算进来（实测 6+1 断言成 12）。
→ 阶段之间必须 `await get_tree().physics_frame` ×2 + 强制清场。

**待办**：`WaveSystem.DEBUG_LOG` 现为 true（新行为待手玩验收后关，与⑤同例）。
下一步 = ⑦ 改装阶段（换炮塔类型 / 扩展槽位）；⑥ 仍冻结等模型。

---

## 9/8 下午 · 手玩验收通过 + 亮度调整 + 改装过场构想入档

**⑤+⑨ 手玩验收通过**（李实机：撑过两波，手感 OK）。`wave_system.gd` DEBUG_LOG
可以关了，**还没关**——下次会话开工第一件事关掉。

**亮度调整**（李实测"太黑看不清敌舰"）：根因 = 敌舰纯 albedo 无 emission，
ambient(0.13)×energy(1.0) 下敌舰亮度 <0.12。`presentation.json` 提亮：
ambient_color→[0.30,0.33,0.40]、energy→1.8、bg→[0.03,0.035,0.06]（总亮度约 ×4，
冷蓝调保留）。**若实机仍暗，下一步给敌舰加 emission**（需顺手把 enemy 视觉数值外置）。

**改装过场构想入档**（李口述 → 我整理）：
- `04_presentation.md` 新增 §十「改装过场流程（提案 · 待拍板）」：REFIT 从"界面"
  变成"地方"（《Hades》结构），流程 4 步（飞维修站 → 维修师对话 → 切炮塔位选型 →
  换装特效），制作路线分两阶段——**A 白盒版无模型依赖**（黑屏过渡+2D 对话框+
  真换装+Tween 粒子特效）、**B 3D 演出版等模型**（Path3D 飞行+角色镜头，与 ⑥ 同批解锁）。
- **Q15 已挂**（99_decisions + 04 §九）：维修师"花多少钱"台词 vs **DEC-033 无经济**——
  ① 纯氛围（提案倾向）② 真经济推翻 DEC-033。⑦ 开工前李必须拍板。
- 技术约束写死在档里：过场不新增全局状态（REFIT 入场演出 + cutscene_started/ended
  屏蔽输入），时长/跳过旋钮外置 presentation.json 的 cutscene 段。

**指针**：拍板后回填 DEC-041。改装功能本体（⑦）与过场演出的先后关系已写进 §十。

---

## 9/8 晚 · ⑦ 改装阶段 = 维修站过场（阶段 A）做完

**李拍板两件事**：Q15 = ① 台词纯氛围（不涉钱，守 DEC-033）；
过场跳过——他问「2-3 秒也要吗」，结论**要但不给按钮**（隐形能力：任意键补完当前段）。

**新增文件**：`data/refit.json`（台词 4 组 + 时序旋钮 + 维修师人设）、
`scripts/systems/refit_sequence.gd`（阶段机 IDLE→FADE_IN→TRAVEL→MECHANIC→
GARAGE→告别→FADE_OUT）、`scripts/ui/refit_overlay.gd`（CanvasLayer 哑层，代码建 UI）、
`scenes/tests/refit_test.tscn` + `scripts/tests/refit_probe.gd`。

**改动**：`turrets.json` 加 `variants` 段（3 种横向差异副炮 flak/gatling/lance，
DPS 守恒但射速/伤害/射程/耐久互换）；`turret.gd` 加 `visual_color` +
`is_auto_enabled()`（换装后继承开火状态）；`turret_system.gd` 加 group 注册 +
`variants/equipped` 状态 + `swappable_ids/variant_ids/variant_info/equipped_variant` +
**`swap_turret()`**（销毁重建，保留 id/sector/mount）+ 三种特效；
`bridge_whitebox` 挂 RefitSequence、过场期间吞键、镜头跟焦点炮位、HUD 换提示。

**验证**：`refit_test` **33/33**；回归 wave 25/25 · hp 30/30 · round2 21/21；
主场景 headless 无脚本报错。

**踩到并修掉的 3 个真 bug**（详见 godot_pitfalls 新 3 节）：
`var v := dict.get()` 推断成 Variant 被 warnings-as-errors 拦；
三元 `x if a else []` 无法统一 Array[StringName] / Array；
**特效挂 `get_tree().get_root()` 时 add_child 失败**（root 正 setup 子节点）→ 改挂 `_fx_root`。

**待办**：`refit_sequence.gd` DEBUG_LOG 现为 true（与 wave_system 同例，手玩验收后关）。
下一步 = ⑦ 剩余部分（扩展槽位 / 解锁节奏）或回头关 DEBUG_LOG；阶段 B 等模型。

---

## 9/8 午 · 收尾：DEBUG_LOG 全部关闭

李 9/8 实机验收 ⑦ 过场 + 换装「还不错，没啥大问题」，拍板收尾。
关掉 3 个仍为 `true` 的开关：`wave_system.gd`、`refit_sequence.gd`、
`enemy.gd`（⑤ 的攻击日志，注释本就写着「手玩验收后改 false」）。
现在 scripts/ 下 10 个 `const DEBUG_LOG` 全部 `false`，输出干净。
`MEMORY.md` 当前状态段同步删掉「待关日志」三处，日期刷新到 09-08。
剩余缺口不变：⑦ 扩展槽位 / 解锁节奏 · 阶段 B 等模型（与 ⑥ 同批）。
