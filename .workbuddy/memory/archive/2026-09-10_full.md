# 2026-09-10

> 写法纪律：细节以 `docs/bible/99_decisions.md` 的对应 DEC + 专册为权威，
> 本文件只留「结论 + 关键证据 + 指针」。今日曾膨胀到 5.0k，已按预算蒸馏。

## ⑨a 战损报告 · 完成（DEC-044）

- 新增 `damage_log.gd`（**TurretSystem 子节点**）：按**本波**累计每门炮承伤 + **来源按敌类分解** + 被毁 + 扇区汇总。
  **开战清空**而非危机清空（危机清空的话整条 REFIT 链再也读不到）。四出口 = HUD 耐久行后 / 维修站面板 / 维修师台词 / 改装台槽位后。
- `turret_damaged` 从 2 参改 3 参（加 `source_id`，当时零订阅方）→ 已回填 `MODULES.md §3`。
- 踩坑：① 来源**一直在传**，只是被 `_source_id` 压了告警 → 压告警时注释必须写明「将来给谁用」，否则伏笔烂掉；
  ② 列战损**不能用 `slots_of_sector()`**（它过滤掉被毁的，而"这门被打没了"正是最该显示的）；
  ③ `show_mechanic` 内部会 `hide_all()`，战损面板**每句台词都要重设一次**。
- 证据：`damage_test` **37/37**。测试纪律：伤害走 `Turret.take_damage` **真实路径**，不直接 emit（会跳过 `source_id` 传递，坏了也是绿的）。

## ⑨b 陷落演出 · 完成（DEC-045）

- **只做编排**：L1 该路 feed 计数标记 / L2 整屏红 + 红叉 / L3 夺操控 + 拉远 95m 环绕 → RESULT。
  爆炸归 ⑪、警报归 ⑩ —— 三者订阅同一批事件，将来是"往里填特效"不是"推翻重写"。
- 关键决策：标记主信号是**整屏染色**不是文字（项目没配字体，不能押在字能不能渲染上）；
  标记打在**屏幕 mesh 前**（玩家认知状态，不是副炮看到的画面）；**表现层对节点缺失容错**（headless 可测纯逻辑）。
- 踩坑：`crisis_cleared` 参数值被忽略（靠 emit 次数累加）；`all_turrets_destroyed` **含主炮 main**；
  **新 `class_name` 必须先 `--headless --import`** 才进全局类缓存，手编 `.gd.uid` 报 invalid UID。
- 证据：`collapse_test` **18/18**；回归全绿；主场景 headless 0 真错误。

## ⑩ 音频骨架 · 完成（DEC-046）

- 4 总线（`Master→Music/SFX/UI`；bible 写 3 条、知识库第 10 课写 4 条，两处打架 → 采纳 4）+ 16 音效位 + 3 BGM 位。
  **听觉 HUD 才是本作音频的真价值** —— 只有艏部一面舷窗，看不见的五面只能靠耳朵。
- 边界：低频警报走 **EventBus 订阅**（接线表在 `audio.json` 的 `events`，换警报音零业务改动）；高频（开火/命中/受创）
  **直调** `play_sfx(id, sector)`。**零素材可用**：`assets/audio/` 为空时按条目 `tone` 现场合成占位音，正式素材进 `path` 即自动接管。
- 踩坑：**接线键名写错会静默静音**（代码查 `wave_start`、配置键是 `wave_started`）→ 已改缺键主动 warn；
  BGM 交叉淡化**淡入对象写反**；`@warning_ignore("unused_signal")` **实测有订阅方即可删**。
- 证据：`audio_test` **60/60**。`DEBUG_LOG` 暂留 `true`（等李 F5 听一遍再关）。专册 `memory/audio.md`。

## ⑪ 第一层（②闪白 / ③震屏 / ④推镜）· 完成（DEC-047）

- 李拍板的顺序：**便宜的先做、危险的最后**（hitstop 是唯一要动主循环的）。
- **实测发现**：`feel.shake_amplitude` / `zoom_punch_fov` 从 9/1 就写好了，但**全工程零代码读过** → ⑪ 第一件事是**接线**，不是加东西。
- 决策：① 强度是**「事件 → 强度表」**不是全局一个数（自动火力每帧在打，全命中给反馈 = 持续抽搐，踩碎 DEC-021）；
  ② 三层归属**零重叠**（GameFeel 写相机 pos/fov、实体写自己材质、bridge 写 Player）→ **「接管时禁震屏」这个分支不用写**；
  ③ 参数走 `FeelKit` 静态读取、**不做 autoload**；④ hitstop 不用 `time_scale=0`，改关两系统的 `_physics_process`；
  ⑤ **旋钮改名**（旧名 `shake_amplitude` 语义错位：三轴叠加可达 ×√3，不是真上限）。
- 踩坑 6 条（细则见 `game_feel.md`）：抖动**帧率混叠**（60fps 下 >20Hz 变慢晃）· 闪白**必须冷却** · 被毁后冷却**擦回型号色** ·
  每波 `restore()` 要清冷却 · 三轴叠加按**模长夹** · `albedo_color > 1` **实测不被钳制**。
- 证据：`feel_test` **49/49**；回归全绿。新增 `feel_kit.gd` · `game_feel.gd` · `feel_test.tscn` + `feel_probe.gd`。

## ⑪ ⑤ 敌人血量反馈 · 完成（B+C）

- 李拍板 **血条 > 浮动伤害数字**：flak 一发秒战机、lance 一发秒战机+轰炸机 → 数字永远只显示满血数，无决策价值；
  有决策价值的是「还差几发」。方案 = **B 接管时的目标血条 + C 本体随血量变暗**。
- 新增 `HealthKit`（读 `presentation.json` 新 `healthbar` 段，照抄 FeelKit）· `TargetHealthBar`（`CanvasLayer` **层 5**；
  `target_provider` 闭包 = 接管中的炮 → `acquire_target()`，与手动开火 LeadPrediction 同源）· `enemy.gd` 按 `hp_ratio()` 变暗。
  前置：`Enemy.hp_max` 快照 + `scale_hp()`（`wave_system` 的 `e.hp *= s` 已改）+ `EventBus.enemy_damaged(...)`（离散，总线不刷爆）。
- **新坑**：C 层与闪白抢材质 → `_apply_hp_tint` 须先于 `_hit_flash`，闪白中只改 `_base_color` 不写材质；
  闪白还原目标变成「按血量染过色的静止色」（`feel_test` 一条旧断言已按新语义改）。细则 **`healthbar.md`**。
- 顺带定性：headless 主场景的 `texture_2d_get "t" is null` 是 **dummy 驱动固有限制**（三行纯引擎代码可复现），与本次无关。
- 证据：`healthbar_test` **43/43**；回归 feel49 / audio60 / round2 21 / hp30 / wave25 / refit33 / unlock77 / damage37 / collapse18 **全绿**。
- 字体前置（详 `fonts.md`）：零字体配置 → `fallback_font` = 内嵌 Open Sans SemiBold；数字内嵌且等宽；
  **`Font.has_char("伤")`=false 是假阴性**（系统兜底只在 shaping 阶段）→ **验字形只信 shaped text**。
- 下一步：⑥ VFX（程序化占位）· ① hitstop（禁 `time_scale=0`，改关两系统的 `_physics_process`）。

---

**工具环境备注**：本机 bash 的 `dirname` / `tail` / `grep` / `timeout` 不在默认 PATH 上，
开头跑 `export PATH="$PATH:/usr/bin:/bin"` 即可恢复；且 `timeout` 会撞到 Windows 自带的 `TIMEOUT.EXE`，
要写 `/usr/bin/timeout`。（详见 `memory/testing.md`）

## 记忆月度蒸馏（本次）

08-30~09-08 共 9 天 → `history.md`，原文归 `archive/` 后删；日志区只留 09-09 / 09-10。
分册重整：feed 三节 → `monitor_feed`、语言层 → `gdscript_snags`、`tooling` 拆出 `testing`。
详见 `history.md` + `MEMORY.md` 维护纪律。
