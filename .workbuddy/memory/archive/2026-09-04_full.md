# 2026-09-04

## 记忆蒸馏：`2026-09-03.md` 超限修复（7446 → 1997）

**问题**：昨天是监控调试重灾日，日志累积到 7446 字符（预算 4000，186%）——
超预算会被静默截断，而最贵的踩坑条目往往在末尾，风险真实（本项目已发生过）。

**处置**（按记忆纪律：verbose 下沉分册，日日志只留「结论 + 关键证据 + 指针」）：
1. **新建分册 `memory/monitor_feed.md`**（2590 字符）—— 监控是本项目微创新载体、
   坑最密集的子系统，单独成册；`godot_pitfalls.md` 保持"通用 Godot 坑"定位不膨胀（已 91%）。
   内容：白盒外壁几何表（调 mount 必看）+ 4 路当前 mount + near 余量警告 + 材质三键旋钮
   + ALBEDO 公式坑 + 5 条出图链路坑 + DEC-038 待设计裁决。
2. **`2026-09-03.md` 蒸馏重写** → 7 条，每条 3-5 行结论 + 指针，7446 → 1997（50%）。
3. **术语约定固化** `tooling.md §协作约定` 第 9 条：用户说"界面/UI/颜色"默认指**编辑器主题**
   而非游戏 HUD（9/3 误判过一次，白改一轮被回滚）。
4. `MEMORY.md` 索引表新增 `monitor_feed.md` 一行（2235 → 2338，84%）。

**结果**：`_check.py` 全部在预算内（9-03 从 186% → 50%，无文件超限）。

**教训**：单日多次追加日志会静默膨胀 —— 以后每次写日志前先跑
`python .workbuddy/memory/_check.py`，接近 90% 就先蒸馏再追加，别攒到超限。

---

## ③ 六扇区 + 敌人：完成

**范围**（严格 BACKLOG ③「不做开火」+ 用户拍板边界）：只做「生成 → 直飞 → 进攻击距离悬停 →
持续贡献 pressure」，不做扣血/开火（④⑤步）；两种敌人 interceptor + bomber（heavy 留 P2）；
aft 权重 0（DEC-036 推进器高温区）。

**交付**：
- `scripts/utils/sectors.gd`（class_name `Sectors` 静态工具）：`sector_of` 六扇区点积归属 + `pressure_from_count`
- `scripts/systems/enemy.gd`（class_name `Enemy`）：单敌实体，`advance()` 由系统统一推进（省 N 次回调）
- `scripts/systems/enemy_system.gd`（class_name `EnemySystem`）：`spawn_one` 拒绝采样生成 / 移动 / 扇区归属 / 压力广播
- `data/enemies.json`：敌人参数单一真源（从 presentation.json 迁移，原 enemy 段已删）
- `scenes/tests/enemy_test.tscn` + `scripts/tests/enemy_test.gd`：独立验证 **15/15 OK**
- 集成进 `bridge_whitebox.tscn`（EnemySystem 节点）+ `KEY_E` 调试生成键（波次调度是独立 P0，未接）

**关键坑（3 个，全实测）**：
1. `Node3D.global_position` 在节点**进树前**读取永远 (0,0,0)（get_global_transform ERR_FAIL 返回单位阵）
   → 必须先 `add_child` 再 `setup`，否则扇区全判成同一面。
2. `queue_free` 的敌人残留在 `_enemies` 数组 → 下帧 `_physics_process` 调已释放实例崩溃
   → `tree_exited` 信号自动出列。
3. `--script` 模式**不加载 autoload**；新建 `class_name` 脚本要跑 `--import` 重建全局类缓存，
   否则 Parse Error「class not found」（`global_script_class_cache.cfg` 初始为空）。

**验证**：enemy_test 15/15；桥场景 headless 零 error（`[diag]/[capture] 返回空` 是 dummy 渲染器副作用）。

**待办**：压力公式是③阶段临时版（威胁数/`pressure_reference_count`），⑤ 接真实炮塔耐久时要换成
bible 的「威胁值 / 剩余防御力」——`sectors.gd` 里已注明两处调用方一起改。
