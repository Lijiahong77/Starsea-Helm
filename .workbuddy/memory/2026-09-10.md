# 2026-09-10

> 写法纪律：细节以 `docs/bible/99_decisions.md` 的对应 DEC + 专册为权威，
> 本文件只留「结论 + 关键证据 + 指针」。（曾两度膨胀，已二次蒸馏；原文见 `archive/2026-09-10_full.md`。）

## ⑨a 战损报告 · 完成（DEC-044）

- 一份数据四出口落地：`damage_log.gd`（**TurretSystem 子节点**）记**本波**承伤 + **按敌类分解** + 扇区汇总；
  **开战清空**（不是危机清空，否则整条 REFIT 链读不到）。四出口 = HUD 耐久行后 / 维修站面板 / 维修师台词 / 改装台槽位后。
- 三坑：① 来源一直在传、被 `_source_id` 压了告警（**压告警必须注释「将来给谁用」**）；
  ② 列战损**不能用 `slots_of_sector()`**（被毁的会被过滤掉）；③ `show_mechanic` 内部 `hide_all()`，每句台词要重设一次。
- 证据：`damage_test` **37/37**。测试走 `Turret.take_damage` 真实路径。细则 → `damage_collapse.md`。

## ⑨b 陷落演出 · 完成（DEC-045）

- **只做编排**：L1 该路 feed 计数标记 / L2 整屏红 + 红叉 / L3 夺操控 + 拉远 95m 环绕 → RESULT。
  爆炸归 ⑪、警报归 ⑩ —— 三者订阅同一批事件，将来是「往里填特效」不是「推翻重写」。
- 关键决策：主信号是**整屏染色**不是文字（项目没配字体）；标记打在**屏幕 mesh 前**（玩家认知状态，非副炮画面）；
  表现层**对节点缺失容错**（headless 可测纯逻辑）。
- 证据：`collapse_test` **18/18**；回归全绿；主场景 headless 0 真错误。细则 → `damage_collapse.md`。

## ⑩ 音频骨架 · 完成（DEC-046）

- 4 总线（bible 写 3 条、知识库第 10 课写 4 条，两处打架 → 采纳 4）+ 16 音效位 + 3 BGM 位。
  **听觉 HUD 才是本作音频的真价值** —— 只有艏部一面舷窗，看不见的五面只能靠耳朵。
- 边界：低频警报走 **EventBus 订阅**（接线表在 `audio.json` 的 `events`，换警报音零业务改动）；
  高频（开火/命中/受创）**直调** `play_sfx(id, sector)`。**零素材可用**：`assets/audio/` 为空时按条目 `tone`
  现场合成占位音，正式素材进 `path` 即自动接管。
- 踩坑：**接线键名写错会静默静音**（代码查 `wave_start`、配置键是 `wave_started`）→ 已改缺键主动 warn；
  BGM 交叉淡化**淡入对象写反**；`@warning_ignore("unused_signal")` **实测有订阅方即可删**。
- 证据：`audio_test` **60/60**。`DEBUG_LOG` 暂留 `true`（等李 F5 听一遍再关）。专册 → `audio.md`。

## ⑪ 手感第一层（②闪白 / ③震屏 / ④推镜）· 完成（DEC-047）

- 李拍板的顺序：**便宜的先做、危险的最后**（hitstop 是唯一要动主循环的）。
- **实测发现**：`feel.shake_amplitude` / `zoom_punch_fov` 从 9/1 就写好了，但**全工程零代码读过**
  → ⑪ 第一件事是**接线**，不是加东西。
- 决策：① 强度是**「事件 → 强度表」**不是全局一个数（自动火力每帧在打，全命中给反馈 = 持续抽搐，踩碎 DEC-021）；
  ② 三层归属**零重叠**（GameFeel 写相机 pos/fov、实体写自己材质、bridge 写 Player）→ **「接管时禁震屏」这个分支不用写**；
  ③ 参数走 `FeelKit` 静态读取、**不做 autoload**；④ hitstop 不用 `time_scale=0`，改关两系统的 `_physics_process`；
  ⑤ **旋钮改名**（旧名 `shake_amplitude` 语义错位：三轴叠加可达 ×√3，不是真上限 → `shake_max_offset`）。
- 踩坑 6 条（混叠 / 闪白冷却 / 被毁擦回型号色 / 每波 restore 清冷却 / 三轴按模长夹 / `albedo_color>1` 不被钳制）
  细则 → `game_feel.md`。
- 证据：`feel_test` **49/49**；回归全绿。新增 `feel_kit.gd` · `game_feel.gd` · `feel_test.tscn` + `feel_probe.gd`。

## ⑪ ⑤ 敌人血量反馈 · 完成（B+C）

- 李拍板 **血条 > 浮动伤害数字**：flak 一发秒战机、lance 一发秒战机+轰炸机 → 数字永远只显示满血数，无决策价值；
  有决策价值的是「还差几发」。方案 = **B 接管时的目标血条 + C 本体随血量变暗**。
- 新增 `HealthKit`（读 `presentation.json` 新 `healthbar` 段，照抄 FeelKit）· `TargetHealthBar`（`CanvasLayer` **层 5**）·
  `enemy.gd` 按 `hp_ratio()` 变暗。前置：`Enemy.hp_max` 快照 + `scale_hp()` + `EventBus.enemy_damaged(...)`。
- **新坑**：C 层与闪白抢材质 → `_apply_hp_tint` 须先于 `_hit_flash`，闪白中只改 `_base_color` 不写材质。
  细则 → `healthbar.md`。
- 证据：`healthbar_test` **43/43**；回归 feel49 / audio60 / round2 21 / hp30 / wave25 / refit33 / unlock77 / damage37 / collapse18 **全绿**。
- 字体前置（→ `fonts.md`）：零字体配置 → `fallback_font` = 内嵌 Open Sans SemiBold；数字内嵌且等宽；
  **`Font.has_char("伤")`=false 是假阴性**（系统兜底只在 shaping 阶段）→ **验字形只信 shaped text**。
- 下一步：⑥ VFX（程序化占位）· ① hitstop（禁 `time_scale=0`，改关两系统的 `_physics_process`）。

---

**工具环境备注**：本机 bash 的 `dirname` / `tail` / `grep` / `timeout` 不在默认 PATH 上，
开头跑 `export PATH="$PATH:/usr/bin:/bin"` 即可恢复；且 `timeout` 会撞到 Windows 自带的 `TIMEOUT.EXE`，
要写 `/usr/bin/timeout`。（详见 `testing.md`）

## 版本控制 · 项目上 GitHub（完成）

- 起因：李问「要不要上 GitHub」，真实诉求是**「想让别人看到我在用 AI 做开发」**（不是备份）→ 选**公开**主仓库。
- 落地（同会话）：`git init -b main` → **143 文件首次提交 `41f5792`** → 公开仓库
  `github.com/Lijiahong77/Starsea-Helm` 已推送。提交身份 `Li Jiahong <noreply 隐私邮箱>`，**只配 --local，没动全局**。
  仓库总 9.14 MB，`assets/` 仍空。**未开 LFS**（源文件别进 git 的规矩已记）。
- 关键修正：原 `.gitignore` 是 **Godot 3 写法**（错误忽略 `*.import`）→ 按官方 4.1+ 重写为
  只忽略 `.godot/` + `*.translation`，另补 `.gitattributes`。环境坑 3 条 → **`tooling.md` 新增「Git / GitHub」一节**（细节不写本册）。
- **待办**：① **README** —— 现在仓库首页是光秃秃的文件列表，这是「被看见」最亏的一环；② 本册里这段对话元信息如要清理再清。
- ⚠ 本册已超预算，**下次落笔前先蒸馏**。

## README + 头图（完成）

- 头图定「**斜后方广角**」而非第一人称 —— 第一人称看不出「只有一面窗」的压迫感，广角能。李用豆包生成。
  处理：水印在 `x1733–1901 / y1020–1055`（原图 1920×1078）→ **裁底部 64px** 得 1920×1014（1.893:1），
  存 `docs/media/hero.png`（1.53 MB）。**已核验水印区 0 亮点** —— 只裁不修图，故不引入涂抹感。
- **README 已建**（`README.md`）：中文为主 + 顶部一句英文。重心不写技术栈，写**「这个项目是怎么做的」**
  （三轮对话节奏 / 五条铁律 / DEC 摘录 / 数值外置 / 408 断言）—— 这是「被人看见在用 AI 做开发」的真正接口。
  诚实标注：概念图 AI 生成、非游戏内截图；当前无美术、`assets/` 空。
- ⭐ **实测数字刷新**：跑全套得 **11 套 / 408 断言全绿**（feel49 · audio60 · hp43 · unlock77 · damage37 ·
  refit33 · hp_test30 · wave25 · round2 21 · collapse18 · enemy15）。此前热区写「8 套 342」**是错的**。
  成因：汇总脚本用 `grep -oE '[0-9]+/[0-9]+'` 只能抓「断言: n/m」那 7 套，后 4 套打「结果：通过 n」→ **静默漏 91 条**
  （0 命中和漏抓长得一样）→ 已补进 `testing.md` 坑 5。同时补上该册漏列的两套测试 + 整套运行命令。
- **待办**：① **License 未定**（公开仓库默认「保留所有权利」，与「欢迎参考」措辞不符，李拍板）；
  ② `docs/PROCESS.md` **严重滞后**（仍写「你在这里 = 第④步」，实际 P0 只剩 VFX + hitstop）—— 建议蒸馏重写。


## 记忆维护（本次）

- 日志两度膨胀 → **二次蒸馏**：⑨a/⑨b 细节下沉**新册 `damage_collapse.md`**（原文 `archive/2026-09-10_full.md`）。
- **热区索引瘦身（李点名）**：索引表曾吃热区 **42.9%**（+66 字符/册，再 6 册就爆）→ 改「**文件名即索引**」：
  只留「高频必读 4 项」+ 模块专册**每册一个词**；分册**首行统一「触发时机」**。热区 2417 → **1917（68%）**；
  机制 = `_check.py` 加「索引小节预算 700」+ 打印分册目录。
- **李拍定分组**：三维度保留「高频必读 / 模块专册 / 历史」，**否决按阶段分**（各阶段册数不均，多的那档还得再分层）；
  模块专册的轴改用 **「玩家能感知 / 纯工程」**（原「表现/系统」作废）。已进热区维护纪律 + skill **v1.4.0**。
