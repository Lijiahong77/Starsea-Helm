# Starsea Helm（星海舵手）· 热区索引
> 每会话自动注入 · 预算 **2800 字符**；超了会被**静默截断**。只放「宪法 + 状态 + 指针」。2026-09-10

## 三条最高宪法
1. **70% 乐趣在装配，30% 在战斗。** 低 APM、允许发呆是设计目标；失败模式 = 决策无意义（随机 vs 认真装配的存活危机数差 < 3）· DEC-021
2. **知识库 `docs/开发指导/` 只给流程不给答案。** 三问法 / Bible / P0–P3 / 一次只改一件事 / 三轮对话节奏必须照做 · DEC-023
3. **数值只给旋钮不给终值**，一律外置 `data/*.json`，禁止 hardcode。

## 一句话定位
第一人称舰桥据点防守 + 波次生存。**微创新 = 只有艏部一面舷窗** —— 信息不对称由物理保证（DEC-026）。单位 = **危机**，动词 3 个：装配 / 接管 / 扫视。

## 当前状态（2026-09-10）
- **P0 总览**（详 `docs/BACKLOG.md`）：①-⑧ + ⑨a/⑨b/⑩ ✅；⑪ 完成 ②③④⑤，余 ⑥VFX / ①hitstop；⑥ 监控冻结（等美术模型）。
- **测试**：P0 共 **11 套 408 断言全绿**（清单 + 运行命令详 `testing.md`）。
- **⑪ 手感 🟡**（DEC-047）：②闪白 / ③震屏 / ④推镜（`FeelKit` 读 `presentation.json` 的 `feel`；`GameFeel` 挂 bridge 只写相机；强度按**「事件→强度表」**分发，自动命中不给）+ ⑤**血量 B+C**（接管目标血条 + 本体变暗，弃浮动数字）。
- **仓库**：公开 `github.com/Lijiahong77/Starsea-Helm` · README 已建 · 头图 = 豆包生成概念图（`docs/media/hero.png`）。
- **缺口 / 下一步**：⑪ ⑥VFX + ①hitstop · **头号重点 = 美术资产方案**（李主导；替换序 舱室→控制台→敌人→炮塔→VFX）。
- ⚠️ **hitstop 禁用** `Engine.time_scale=0` → 改关 EnemySystem/TurretSystem 的 `_physics_process`（世界推进集中式）。
- ⚠ 未决：fore 来袭先啃 dorsal（49.6m）而非主炮（51.2m）—— bible「最近」未限扇区（改 `Enemy._attack_tick`）。

## 分册在哪、怎么找
> 全在 `.workbuddy/memory/`。**每册首行 self-describing（写了触发时机）**，拿不准就先 Glob 目录看文件名。

- **高频必读**（跨阶段，几乎每会话都要）：`testing` 跑验证/写探针 · `tooling` 改文件/接 MCP 前 ·
  `godot_pitfalls` + `gdscript_snags` 写 `.gd`/`.tscn` 前 · `docs/bible/` 设计问题唯一权威（含 DEC 编号）。
- **模块专册**（动某模块前读；分组轴 = **玩家能否感知**；**新增册只加一个词，绝不加整行**）：
  玩家能感知 `game_feel`·`healthbar`·`damage_collapse`·`monitor_feed`·`fonts`·`audio` ‖ 纯工程 `design_snapshot`
- **历史**：`history` 逐日大事记（追溯决策怎么来的）· `YYYY-MM-DD` 最近 1–2 天日志（接手用）。

## 维护纪律
- **预算**：本文件 ≤ 2800 · 分册 ≤ 6000 · 日志 ≤ 4000。**新内容默认写分册，本文件只加指针**。
- **加新分册 → 只在上面「模块专册」加一个词（≤12 字符）**，不要加一整行表格 —— 索引本身也有预算。
- **分册文件名必须自解释**（`audio` 行、`notes` / `misc` 不行）—— 文件名就是索引。
- **分组轴固定为「玩家能否感知」**：别改成按阶段分（各阶段册数不均，多的那档还得再分层）。
- 去重：设计细节以 `docs/bible/` 为唯一权威；分册只放「够用快照」。
- 日志：一条 3–5 行「结论 + 证据 + 指针」；每月蒸馏进分册 + `history`，原文归 `archive/`（2026-09-10 已做一次）。
- 自查：`python .workbuddy/memory/_check.py`。
